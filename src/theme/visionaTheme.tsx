import { createTheme } from "@mui/material";

export const visionaTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#F18419",
    },
    secondary: {
      main: "#1942ac",
    },
    background: {
      default: "#19212C",
    },
  },
});
